---
description: run and build
---

# Build and run

`./scripts/run_server.sh`

# Skip build (if already built)

`./scripts/run_server.sh --skip-build`

run one of these two depending on if we made changes recently (and build) vs just remaking the JAR. usually we want `./scripts/run_server.sh`
